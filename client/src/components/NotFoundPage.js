import React from 'react';

/**
 * Not found page
 * 
 * @returns not found component 
 */
const NotFoundPage = () => {
    return <h2 style={{color: "white"}}>Oops, we cannot find the page you are looking for.</h2>;
} 
export default NotFoundPage;